CREATE TABLE PAYS (
PAYS_ID BIGINT AUTO_INCREMENT PRIMARY KEY,
LIBELLE VARCHAR(255)
);
CREATE TABLE PROFIL (
PROFIL_ID BIGINT AUTO_INCREMENT PRIMARY KEY,
LIBELLE VARCHAR(255) NOT NULL UNIQUE
);






